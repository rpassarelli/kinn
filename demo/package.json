{
  "name": "kinn3-demo",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "astro dev --host 0.0.0.0",
    "build": "astro build",
    "preview": "astro preview --host 0.0.0.0",
    "data": "cd .. && uv run python demo/scripts/build_data.py -o demo/src/data/session.json calibration-runs/benchmark/dental-clinic",
    "test": "playwright test",
    "test:install": "playwright install chromium"
  },
  "dependencies": {
    "astro": "^5.1.0",
    "gsap": "^3.15.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "typescript": "^5.6.0"
  }
}
